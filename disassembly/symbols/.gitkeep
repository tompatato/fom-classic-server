# Reverse-engineered symbol shards land here, one dir per client PE module
# (symbols/<program>/…), generated by `just ghidra-dump`. Empty until recon
# Step 0 catalogues the 2006 client. See ../README.md.
